int main()
{
в пространстве имен NODE : 
создать структуру node, которая содержит две переменные
int id{}
node* next_node{nullptr}

объявить и определить функцию bool chaining(node* n1, node* n2)
таким образом : 
если n1->next_node не равен nullptr, вернуть false
иначе присвоить адрес n2 переменной n1->next_node

в функции main() :
объявить массив NODE::node nodes[10]
присвоить значение переменным id (от 100 до 1000) в каждом элементе массива
присвоить адрес iтого 


}